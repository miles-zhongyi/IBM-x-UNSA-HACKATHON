# Optional — ingestion + chat LLM (IBM watsonx.ai)
WATSONX_API_KEY=
WATSONX_PROJECT_ID=
WATSONX_URL=https://us-south.ml.cloud.ibm.com
WATSONX_MODEL_ID=ibm/granite-3-8b-instruct

# Optional — force hashed embeddings (CI / offline)
# EMBEDDING_BACKEND=hashed

# Dev only — wipe SQLite/Chroma + re-seed demo on next API startup
# DEMO_SEED_FRESH=1
